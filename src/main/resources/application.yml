spring:
  application:
    name: monitoring

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,loggers, prometheus
  endpoint:
    health:
      show-details: always

      # localhost:8080/actuator ? actuator? ?? ???
